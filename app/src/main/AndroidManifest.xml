<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Media permissions for Hidden Gallery -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- For Android 11+ to access external storage -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Biometric permissions for Fingerprint Authentication -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:name=".CalculatorApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OneCalculator"
        android:requestLegacyExternalStorage="true">

        <activity android:name=".BasicActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LauncherActivity" android:exported="true" />
        <activity android:name=".MainActivity" android:exported="true" android:screenOrientation="portrait" />
        <activity android:name=".HomeActivity" android:screenOrientation="portrait" />
        <activity 
            android:name=".HistoryActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait" />

        <activity android:name=".OnboardingActivity" />

        <activity android:name=".SettingsActivity" android:screenOrientation="portrait" />

        <activity android:name=".MainTabActivity" android:screenOrientation="portrait" />

        <activity android:name=".mathly.ScannerDetailsActivity" android:screenOrientation="portrait" />

        <activity android:name=".AboutUsActivity" />
        <activity android:name=".PrivacyPermissionsActivity" />
        <activity android:name=".CheckUpdateActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".RateUsActivity" />
        <activity android:name=".TodoActivity" />
        <activity android:name=".HotAppsActivity" />
        <activity android:name=".MediaGalleryActivity" android:screenOrientation="portrait" />
        <activity android:name=".FolderContentsActivity" android:screenOrientation="portrait" />
        <activity android:name=".NewFolderContentsActivity" android:screenOrientation="portrait" />
        <activity android:name=".ImageViewerActivity" android:screenOrientation="portrait" />

        <receiver android:name=".CalculatorWidgetBlack" android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/calculator_widget_info" />
        </receiver>

        <service
            android:name=".FloatingMicService"
            android:exported="false" />

        <receiver
            android:name=".TabBarVisibilityReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.shaadow.onecalculator.UPDATE_TAB_BAR_VISIBILITY" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

    </application>

</manifest>
